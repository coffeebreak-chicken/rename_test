ファイル名を（一部）変更します
ファイルの内容も一応変更します。

・変更したいファイルが存在するディレクトリのパスを指定して下さい。
・ファイル名の全体を変更しません。
・変更対象のファイル名を入力します。
    入力したファイル名がフォルダに存在するか確認します。（正規表現による部分一致）
・変更後のファイル名を入力します。
    上記の、変更対象のファイル名の"部分"をここで入力した変更後のファイル名に変更します。

